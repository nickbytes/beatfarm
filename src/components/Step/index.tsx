import * as React from "react";
import styled from "styled-components";

const StepSpan = styled.span`
  width: 16px;
  height: 16px;
  margin: 0 10px;
  background-color: rgba(62, 111, 173, 1);
`;

const Step = () => <StepSpan />;

export default Step;
